# ===== Required =====
# Spring Backend base URL (e.g. http://localhost:8080)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Turn on Next.js -> Spring proxy for dev (true/false).
NEXT_PUBLIC_DEV_PROXY=true

# If you use Bearer tokens, put the header name here (usually 'Authorization').
NEXT_PUBLIC_AUTH_HEADER=Authorization
